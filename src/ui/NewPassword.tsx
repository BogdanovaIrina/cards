import React from 'react';
import {NavLink} from "react-router-dom";

export const NewPassword = () => {
    return (
        <div>
            <NavLink to={'/404'}>New-password</NavLink>
        </div>
    )
}